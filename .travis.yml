language: python
python:
    - "3.6"

# Install dependencies
install:
    - pip install -r requirements.txt

# Run tests
script:
    - pytest
    - coverage run --source=api.v1.resources -m pytest && coverage report

after_success:
    - coveralls
